# Plan: Restore and Fix Workflow "10 SOCIAL BRIGHTDATA TRIGGER"

## 🚨 CRITICAL ISSUE
The workflow has been BROKEN by previous edits. It now only contains ONE node (LogProcessingEnd) and has ZERO connections. The workflow-map shows: "Nodes: 1 | Connections: 0"

## Goal
Completely RESTORE the full workflow while keeping the new logging functionality the user requested:
- Log at the START of download (with lote=snapshot_id, ruta=file_path, procesado=false)
- Log at the END of processing (with procesado=true)

## Current Broken State
The workflow is essentially empty - only LogProcessingEnd node exists with no routing.

## Restoration Plan

### Step 1: Restore All Original Nodes
Rebuild the complete workflow structure with all original nodes:

1. **ManualTrigger** - Manual trigger to start
2. **GetActiveAccounts** - PostgreSQL query to get active Facebook accounts
3. **TriggerBrightdata** - HTTP request to trigger BrightData scraping job
4. **CheckProgress** - HTTP request to poll BrightData for job completion status
5. **IsReady** - IF node to check if status === 'ready'
6. **WaitBeforeRetry** - Wait node for retry loop (360 seconds)
7. **NEW: PrepareSnapshotPath** - Code node to build file path and create directory
8. **NEW: LogDownloadStart** - PostgreSQL insert to log start (lote, ruta, procesado=false)
9. **DownloadSnapshot** - HTTP request to download NDJSON snapshot
10. **BuildSnapshotFilePath** - Code node to prepare binary data for file writing
11. **WriteSnapshotFile** - File write node (with continueErrorOutput)
12. **ConvertNdjson** - Code node to parse NDJSON and extract posts
13. **SendToIngest** - HTTP request to send posts to ingest endpoint
14. **LogProcessingEnd** - PostgreSQL update to mark procesado=true
15. **LogTrigger** - PostgreSQL insert to log trigger event details
16. **SummarizeTrigger** - Code node to summarize results

### Step 2: Restore Complete Routing
All connections including:
- Main flow: ManualTrigger → GetActiveAccounts → TriggerBrightdata → CheckProgress → IsReady → PrepareSnapshotPath → LogDownloadStart → DownloadSnapshot → BuildSnapshotFilePath → WriteSnapshotFile → ConvertNdjson → SendToIngest → LogProcessingEnd → LogTrigger → SummarizeTrigger
- Retry loop: IsReady (out:1) → WaitBeforeRetry → CheckProgress
- Error output: WriteSnapshotFile (out:1) → ConvertNdjson

### Step 3: Validate and Test
- Run schema validation
- Push to n8n
- Verify workflow structure

### Key Questions for User
1. Should I also add the NEW logging at download START as previously requested?
2. Are there any other enhancements you want added while restoring?
3. Do you want me to verify with a test execution after restoration?</content>
<parameter name="filePath">/srv/dev/sgae/n8n/.kilo/plans/1775987226692-clever-sailor.md