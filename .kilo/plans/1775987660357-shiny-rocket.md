## Implentation Plan: New Workflow from antiquos/10 SOCIAL BRIGHTDATA TRIGGER.json

### 1. JSON Structure Analysis
- Review 455-line structure to identify:
  - Key nodes: Manual Trigger, Get Active Accounts, Trigger BrightData, Download Snapshot, Convert Ndjson, Send To Ingest, Log Trigger
  - Critical parameters: 
    - Postgres Credentials: `E2XU4m84S5WN82lK`
    - BrightData Credentials: `0rTspKriNsoJizFz`
    - URL patterns: 
      - BrightData API: `https://api.brightdata.com/datasets/v3/{endpoint}`
      - Ingest Endpoint: `https://ingesta.sistechvision.online/webhook/social/brightdata/facebook`
- Connection paths:
  - Manual Trigger → Get Active Accounts → Trigger BrightData
  - Error Handling: WaitBeforeRetry → CheckProgress loop

### 2. Implementation Priorities
- Preserve JSON's exact node configurations
- Validate credential IDs match current setup
- Ensure URL parameters in Trigger BrightData match JSON format
- Replicate error handling pattern

### 3. Workflow Structure
```mermaid
graph TD
  A[Manual Trigger] --> B[Get Active Accounts]
  B --> C[Trigger BrightData]
  C --> D[Download Snapshot]
  D --> E[Convert Ndjson]
  E --> F[Send To Ingest]
  C --> G[WaitBeforeRetry] --> C (loop)
  D --> H[Log Trigger]
```

### 4. Validation Steps
- Run `npx n8nac skills validate` on new workflow
- Test full cycle manually 3 times
- Verify database entries in:
  - Postgres processing_logs table
  - Ingest endpoint logs

### 5. Monitoring Strategy
- Enable audit logging for:
  - Trigger BrightData (API calls)
  - Send To Ingest (data volume)
  - Log Trigger (status transitions)
- Set timeout alerts for CheckProgress node (360s)

Would you like me to:
1. Create the workflow file now?
2. Focus on credential validation first?
3. Address specific URL/parameter concerns?

<next_step>Proceed with implementation</next_step>