# Plan completado: `35 SOCIAL MEDIA RESET DOWNLOAD` modificado para reset por año + ayuntamiento

## ✅ Implementación finalizada

Se han implementado todas las correcciones necesarias para cumplir con el objetivo de reset por año y ayuntamiento elegido.

### 🔧 Cambios realizados:

1. **Reemplazada la query debug** por la query de reseteo real con validaciones tempranas
2. **Corregido el workflow-map** para mostrar correctamente 3 nodos y 2 conexiones
3. **Eliminada la duplicación de `@links()`** que causaba conflictos
4. **Agregada validación de entradas** mediante CTE `validated_input` que:
   - Verifica que `ayto_id` y `year` no sean NULL/vacíos
   - Confirma que `year` tenga formato de 4 dígitos
   - Retorna NULL si los parámetros son inválidos (previniendo reseteos accidentales)
5. **Mantuvieron las mejoras de seguridad**:
   - Uso de `LOWER()` para comparación case-insensitive del ayto_id
   - Uso de parámetros `$1`, `$2` para prevenir SQL Injection
   - `FOR UPDATE SKIP LOCKED` para evitar race conditions

### 📊 Estado actual del workflow:

- **Archivo**: `workflows/192_168_0_50:5678_rafael_g/personal/social_media_reset_download.workflow.ts`
- **Estado**: Publicado y verificado en n8n
- **Validación**: `npx --yes n8nac skills validate` → ✅ Workflow is valid!
- **Publicación**: `npx --yes n8nac push --verify` → ✅ Publicado y verificado exitosamente
- **Sincronización**: Estado **TRACKED** en n8n

### 🚀 Pruebas realizadas:

El workflow ha sido probado exitosamente con:
- `{ ayto_id: 'SantaFedeMondujar', year: 2025 }` (caso específico del usuario)
- `{ ayto_id: 'pechina', year: 2024 }` 
- `{ ayto_id: 'viator', year: 2025 }`

### 📋 Próximos pasos para usuarios:

Para ejecutar el reset, use el Manual Trigger con:
```json
{
  "ayto_id": "SU_AYTO_ID_AQUI", 
  "year": SU_AÑO_AQUI
}
```

Ejemplos válidos:
- `{ "ayto_id": "SantaFedeMondujar", "year": 2025 }`
- `{ "ayto_id": "pechina", "year": 2024 }`
- `{ "ayto_id": "viator", "year": 2025 }`

### ✅ Resultado esperado:

El workflow ejecutará el reseteo real de:
- `social_posts.media_status` → 'MEDIA_PENDING'
- `post_media.download_status` → 'MEDIA_PENDING'

Solo afectará registros del ayuntamiento y año especificados, retornando contadores diagnósticos:
- `posts_matched`: Número de posts que coinciden con el filtro
- `posts_reset`: Número de posts cuyo status fue actualizado
- `media_rows_reset`: Número de filas de media cuyo download status fue actualizado

Si los resultados son cero, puede deberse a:
1. No existen posts para ese ayto_id/año combinación
2. Los posts existen pero ya están en estado MEDIA_PENDING
3. El ayto_id no existe en publisher_accounts (use minúsculas para coincidencia)

### 🎯 Objetivo cumplido:

✅ **Reset por año y ayuntamiento elegido** - IMPLEMENTADO Y VERIFICADO

